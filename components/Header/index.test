import { render } from '@testing-library/react-native';

const { Header } = require('.');

describe('Header', () => {
  test('It should render Header component', () => {
    const { toJSON } = render(
      <Header cityName="Test City" countryName="Test " />,
    );
    expect(toJSON()).toMatchSnapshot();
  });
});
